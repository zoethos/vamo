name: vamo_workspace
description: Vamo monorepo root (melos-managed). Not a publishable package.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  melos: ^6.1.0
  http: ^1.2.2
  supabase: ^2.5.3
