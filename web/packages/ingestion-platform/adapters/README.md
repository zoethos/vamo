# Adapter Modules

Adapters are the only boundary to external sources, target databases, and
transforms that depend on non-core behavior. Core code calls adapter interfaces;
adapters do not redefine platform policy.
