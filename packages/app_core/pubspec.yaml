name: app_core
description: Cross-cutting Vamo foundations — Supabase, auth, design system, router, analytics.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"  # Dart 3.6 = Flutter 3.27+ (Color.withValues)
  flutter: ">=3.22.0"

dependencies:
  drift: ^2.20.0
  drift_flutter: ^0.2.0
  flutter:
    sdk: flutter
  connectivity_plus: ^6.0.5
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  http: ^1.2.2
  path: ^1.9.0
  package_info_plus: ^8.0.2
  path_provider: ^2.1.4
  posthog_flutter: ^5.25.0
  supabase_flutter: ^2.5.6
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.12
  drift_dev: ^2.20.0
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  sqlite3: ^2.4.0