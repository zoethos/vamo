import 'place_models.dart';

bool get placeGeocodeSupported => false;

Future<GeocodeCoords?> geocodeAddress(String address) async => null;

Future<ResolvedDestination?> resolveDestination(String query) async => null;
